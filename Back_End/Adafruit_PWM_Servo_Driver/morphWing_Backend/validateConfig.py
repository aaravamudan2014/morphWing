#!/usr/bin/python
MAX_ANGLE_TOLERANCE = 10
def main(config_list):
    counter = 0
    for i in range(config_list):
        if i > 1:
            if(abs(config_list[i] - config_list[i+1]) > MAX_ANGLE_TOLERANCE) 
               


def normalize(left,center, right)

